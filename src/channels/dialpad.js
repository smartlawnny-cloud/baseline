/**
 * Baseline — Dialpad Channel (Phase 3)
 * Placeholder until Dialpad API key is configured
 */
var DialpadChannel = {
  connected: false,

  isConnected: function() { return false; },

  fetchCalls: async function(since) {
    // Phase 3: Dialpad REST API integration
    return [];
  },

  toBriefingItems: function(calls) {
    return [];
  }
};
